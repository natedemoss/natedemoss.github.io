# natedemoss.github.io

Personal site — <https://natedemoss.github.io>

Plain HTML and CSS, no build step. Edit `index.html` / `style.css` and push to
`main`; the workflow in `.github/workflows/deploy.yml` uploads the repo root to
GitHub Pages.

The previous Astro blog lives on the `astro-blog-backup` branch.
